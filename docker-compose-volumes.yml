
# New volumes section for persistent Teams auth sharing
volumes:
  teams-browser-data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /app/data/browsers-shared
    # Persisted auth cookies - shared across all containers
# This volume contains the browser data directory with MS Teams OAuth tokens
